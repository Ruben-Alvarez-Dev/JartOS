[pytest]
testpaths = tests
pythonpath = src
addopts = -v --tb=short --ignore=src/tests
markers =
    slow: marks tests as slow
    integration: marks integration tests
collect_ignore_glob = ["src/tests/*"]

[tool:pytest]
testpaths = tests
pythonpath = src
